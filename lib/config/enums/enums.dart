enum AuthState { waiting, notSet }

enum NotificationType { normal, completeFees, ad }

enum RegistrationType { newStudent, registerForNew }

enum StydyLevel { primary, middle, highe }

enum DataState { loading, notSet, error }

enum AcceptanceType { general, engineersChildren, parallel }

enum StudyState { newStudent, zeroFail, failOnce, failTwice }

enum AccountState { active, pending, frozen }

enum MaritalStatus { single, married }

enum StudyLevel { primary, middle, highe }

enum CertificateType { general, vocational, sharia }

enum PhoneVerificationState { SHOW_PHONE_FORM_STATE, SHOW_OTP_FORM_STATE }
